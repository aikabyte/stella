export component HomeView {
    VerticalLayout {
        alignment: center;
        Text {
            horizontal-alignment: center;
            text: "Stella";
            font-size: 40px;
        }

        Text {
            horizontal-alignment: center;
            text: "A comic reader in early development :)";
            font-size: 20px;
        }
    }
}
