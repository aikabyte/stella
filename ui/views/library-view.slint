export component LibraryView {
    VerticalLayout {
        alignment: center;
        Text {
            horizontal-alignment: center;
            text: "Library";
            font-size: 40px;
        }

        Text {
            horizontal-alignment: center;
            text: "No library yet :)";
            font-size: 20px;
        }
    }
}
