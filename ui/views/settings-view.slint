export component SettingsView {
    VerticalLayout {
        alignment: center;
        Text {
            horizontal-alignment: center;
            text: "Settings";
            font-size: 40px;
        }

        Text {
            horizontal-alignment: center;
            text: "Nothing to set yet :)";
            font-size: 20px;
        }
    }
}
